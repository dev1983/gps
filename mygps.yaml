apiVersion: v1
kind: Service
metadata:
  name: mygps
  labels:
    app: mygps
spec:
  ports:
    - port: 8082
  selector:
    app: mygps
    tier: frontend
  type: LoadBalancer
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: gps-pv
spec:
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mygpsapp
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gps-pv-claim
  labels:
    app: mygps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: mygps
  labels:
    app: mygps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mygps
      tier: frontend
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mygps
        tier: frontend
    spec:
      containers:
      - image: dev1983/gps:mygps
        name: mygps
        ports:
        - containerPort: 8082
          name: mygps
        volumeMounts:
        - name: mygps-persistent-storage
          mountPath: /var/www/html
      volumes:
      - name: mygps-persistent-storage
        persistentVolumeClaim:
          claimName: gps-pv-claim
